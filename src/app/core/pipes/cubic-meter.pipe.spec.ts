import { CubicMeterPipe } from './cubic-meter.pipe';

describe('CubicMeterPipe', () => {
  it('create an instance', () => {
    const pipe = new CubicMeterPipe();
    expect(pipe).toBeTruthy();
  });
});
